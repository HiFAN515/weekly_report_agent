# 周报 Agent 全阶段工作清单

> 基于产品方案 V2.1，按依赖顺序排列。
> 状态标记：[ ] 待开发  [~] 进行中  [x] 已完成

---

## Phase 1：MVP（2 周）

> 目标：CLI 跑通 "采集 → 提取 → 渲染" 完整流程
> 期望成果：wkr log --from-git && wkr report 生成一份可读周报

### 阶段 1：脚手架 + 配置

- [x] **1.1** 项目脚手架
  - pyproject.toml（依赖：click, openai, gitpython, faiss-cpu, sentence-transformers, jinja2, python-docx, pydantic, pyyaml, rich）
  - src/__init__.py
  - 包结构：collectors/, storage/, agent/, preprocessor/, generator/, scheduler/

- [x] **1.2** 配置系统
  - config.yaml 默认模板（含 project、repositories、git、security、llm、rag、report、schedule、ingest 全部字段）
  - src/config.py：YAML 加载、${ENV_VAR} 替换、dataclass 结构化、默认值填充

### 阶段 2：数据采集 + 存储

- [x] **2.1** Git 采集器
  - src/collectors/git_collector.py：GitCollector 类
  - GitPython 读取 commit 列表（hash、author、date、message、files_changed、insertions、deletions）
  - 按天聚合、分支策略（merged/all/current）、时区转换（Asia/Shanghai）、author 过滤
  - 支持 per-repo author override（仓库级覆盖全局默认）

- [x] **2.2** Git 统计缓存
  - src/storage/cache_store.py：GitStatsCache 类
  - SQLite 表：commit_stats(hash TEXT PK, repo TEXT, insertions INT, deletions INT, files TEXT)
  - 接口：has(hash) / get(hash) / set(hash, stats) / gc(existing_hashes)

- [x] **2.3** GitCollector 接入缓存
  - 采集时先查 SQLite 缓存，命中直接用，未命中执行 git diff --stat 后写入缓存
  - amend/rebase 时新 hash 自动重算，旧 hash 成为孤立条目（不主动清理，gc 可选）

- [x] **2.4** 低质量 commit 检测
  - 检测规则：长度 < 10 字符，或匹配 /^(fix|update|wip|test)$/i
  - 在日志中标注 [低信息量 commit]，提示用户 wkr log --manual 补充

- [x] **2.5** 日志存储
  - src/storage/log_store.py：LogStore 类
  - 路径规范：logs/git/{repo_alias}/{date}.md、logs/manual/{date}.md
  - 按需生成 _merged/{date}.md（report 时合并，非每次采集都写）
  - 跨仓库同名 commit hash 去重

- [x] **2.6** 日志 Markdown 格式
  - 每个 commit 的渲染模板，按安全级别过滤内容：
    - strict：message + 文件名
    - balanced：message + 文件名 + 增删行数
    - full：+ diff 摘要片段（截断到 max_diff_chars）
  - files_changed = 不重复文件总数（跨仓库同路径算不同文件）

- [x] **2.7** 手动录入
  - src/collectors/manual_collector.py
  - stdin 读取 / 交互式输入 → 写入 logs/manual/{date}.md

### 阶段 3：Agent 核心

- [x] **3.1** Schema 定义
  - src/agent/schema.py
  - 全局 JSON Schema（highlights、daily_work、issues、next_week、data_summary）
  - 校验函数：validate(data) → (bool, list[str])
  - 精确错误信息：字段路径 + 期望类型 + 实际值

- [x] **3.2** System Prompt
  - src/agent/prompts.py
  - ReAct 模式 prompt（含工具使用说明 + JSON 输出格式 + Few-shot 示例）
  - 降级模式 prompt（更直接的指令，无工具调用说明）
  - 原则：数据驱动、成果导向、commit 不足时标注 [需补充]、禁止编造

- [x] **3.3** Agent 工具定义
  - src/agent/tools.py
  - get_week_summary：返回按天聚合的统计摘要（非完整日志）
  - get_day_detail：返回指定日期完整日志
  - get_file_change_summary：文件名 + 增删行数，full 模式含 diff（include_diff 参数由安全级别自动控制）
  - submit_report_data：接收 JSON，触发 Schema 校验
  - 所有工具返回格式与 Schema 对齐

- [x] **3.4** 上下文组装
  - src/preprocessor/context_builder.py：ContextBuilder 类
  - build() 流程：读日志 → 聚合统计 → 安全过滤 → RAG 检索（Phase 1 先跳过，留接口） → token 预算分配 → 组装

- [x] **3.5** Token 预算控制
  - estimate_tokens() 统一工具函数（Phase 1 用字符近似，Phase 2 接入 tokenizer）
  - 预算分配：系统 prompt 2000 + 输出预留 4000 = 固定开销；剩余按 摘要 10% : 历史 20% : 日志 70%
  - 日志优先保留最新一天，其余按时间倒序截断
  - 降级模式：更激进压缩（top_k 3→5 降为 3，每条 500→300 字符，最旧日志丢弃保留最新 3 天）

- [x] **3.6** ContextBuilder.build_debug()
  - --dump-context 专用，输出 token 预算分配详情 + 预处理上下文
  - 完全离线，不调用 LLM

- [x] **3.7** ReportAgent — ReAct 模式
  - src/agent/react_agent.py：ReportAgent 类
  - OpenAI FC 多轮工具调用循环（while + tool_calls）
  - 最后一步调用 submit_report_data 输出 JSON

- [x] **3.8** ReportAgent — 降级模式
  - 单次调用，prompt 注入完整上下文
  - response_format 兼容检测：先尝试 {"type": "json_object"}，不支持则去掉该参数
  - Ollama/通义千问/小模型走这条路径

- [x] **3.9** 健壮 JSON 提取
  - _extract_json_robust() 四级容错：
    1. 直接 json.loads
    2. 正则匹配最外层 { ... }
    3. 去除 ```json``` 标记后重试
    4. 追询 LLM 修正格式（最多 2 次）
  - JsonExtractionError 异常类（含自助排查建议）

- [x] **3.10** FC 自动检测
  - _detect_mode()：云端 API 默认 react，Ollama 先测试一次 FC 调用，失败降级
  - 打印提示：⚠️ 本地模型不支持 Function Calling，切换为降级模式

### 阶段 4：模板渲染 + CLI 串联

- [x] **4.1** 标准模板
  - templates/standard.md.j2：标准研发周报 Jinja2 模板
  - 含核心成果、详细工作（按天）、问题与风险（表格）、下周计划、数据摘要

- [x] **4.2** 模板 Schema
  - templates/standard.schema.yaml：模板二次校验
  - 类型约束（list/object/string/integer）、枚举值、默认值
  - 区分 LLM 输出字段 和 程序自动填充字段（author、year、week_num、date_range）

- [x] **4.3** 模板引擎
  - src/generator/template_engine.py：TemplateEngine 类
  - Jinja2 加载、config 默认值 + LLM 输出合并渲染
  - 变量缺失时用 schema.yaml 中的 default 填充，不报错

- [x] **4.4** 两级 Schema 校验
  - ReportSchemaValidator 类
  - 第 1 级：全局 JSON Schema（字段完整性 + 类型 + 枚举）
  - 第 2 级：模板 Schema 二次校验（子结构完整性 + 模板特有约束）
  - validate_with_retry()：最多 3 次重试，失败时打印精确错误（如 issues[0].status 值 "已完成" 不在枚举中）

- [x] **4.5** 导出器
  - src/generator/exporter.py
  - Markdown 写文件（默认）
  - DOCX 导出（python-docx，Phase 1 可简化）
  - 输出路径：reports/{start}_{end}_weekly_report.md

- [x] **4.6** CLI 框架
  - src/cli.py：Click 主命令 wkr + 子命令注册
  - 子命令：init、log、ingest、report、show、search、schedule、config、template

- [x] **4.7** wkr init
  - 交互式向导：项目名、仓库路径、Git 用户名、模板选择、LLM provider、安全等级、Embedding 模型、定时时间
  - 选择云端 API 时打印数据安全提醒
  - 写入 config.yaml

- [x] **4.8** wkr log 命令
  - --from-git：采集今日 Git 提交（本地仓库，不必 push）
  - --manual：交互式手动输入
  - --file：导入指定文档
  - 采集后询问是否补充说明

- [x] **4.9** wkr report 命令
  - 串联完整流程：ContextBuilder → ReportAgent → SchemaValidator → TemplateEngine → Exporter
  - 参数：--week（指定周）、--template（指定模板）、--preview（打开预览）、--dry-run（输出 JSON 不渲染）、--dump-context（离线调试）
  - 交互：生成后询问 保存/修改/导出DOCX

- [x] **4.10** wkr show 命令
  - 无参数：今日日志
  - --date：指定日期
  - --week：本周汇总
  - 用 rich 美化输出

### 阶段 5：MVP 测试验证

- [ ] **5.1** 端到端冒烟测试
  - wkr init → wkr log --from-git → wkr report → 生成可读周报
  - 验证：Git 统计正确、JSON Schema 校验通过、模板渲染无报错、文件输出到 reports/

- [ ] **5.2** 降级模式测试
  - 配置 Ollama 小模型（如 qwen2.5:7b），跑一遍完整流程
  - 验证：_detect_mode() 自动降级、_extract_json_robust() 四级容错

- [ ] **5.3** --dump-context 测试
  - 验证 token 预算分配正确（摘要/日志/历史比例）
  - 验证安全过滤生效（strict 模式下无文件名、full 模式下有 diff）
  - 验证截断逻辑（超长日志保留最新、标注"共 M 条"）

---

## Phase 2：增强（2 周）

> 目标：补齐数据源和检索能力
> 期望成果：支持多种数据源，周报质量显著提升（有历史上下文支撑）

### 阶段 6：RAG 语义检索

- [ ] **6.1** FAISS 向量索引
  - src/storage/vector_store.py：VectorStore 类
  - FAISS IndexFlatL2 持久化（faiss.write_index / read_index）
  - 存储结构：index/logs.index + index/metadata.json + index/indexed_keys.json
  - 增量更新：新日志只做 embedding 追加，不全量重建

- [ ] **6.2** Embedding 模型加载
  - 支持本地 sentence-transformers 模型（默认 text2vec-base-chinese）
  - 备选：paraphrase-multilingual-MiniLM-L12-v2、bge-small-zh-v1.5
  - 支持云端 Embedding API（api:https://... 前缀格式）
  - 首次加载预下载模型，后续缓存在 ~/.cache

- [ ] **6.3** 子 chunk 拆分
  - 日志类：按天分 chunk，超过 DAILY_CHUNK_THRESHOLD（1500 tokens）时按仓库/来源拆为子 chunk
  - 非日志类文档：通用 chunk_size=300, overlap=50
  - chunk metadata：{date, source, repo, part, indexed_at}

- [ ] **6.4** 整体 embedding 检索
  - search_by_similarity()：本周日志整体作为 query 与历史 chunk 做相似度
  - 同一天多个子 chunk 只保留得分最高 1 条（去重）
  - 超长 query 降级：超过模型 max_seq_length 时自动用摘要替代
  - 排除当前周的数据

- [ ] **6.5** ContextBuilder 接入 RAG
  - build() 中 RAG 检索从"留接口"改为实际调用
  - 检索结果注入上下文的"相关历史背景"部分
  - 与 token 预算联动（历史占 20%）

- [ ] **6.6** 语义搜索命令
  - wkr search "关键词" → 调用 VectorStore.search_by_similarity()
  - 输出：日期 + 相似度得分 + 内容摘要
  - 用 rich 表格美化

- [ ] **6.7** token 估算精确化
  - 接入 tokenizer：tiktoken（OpenAI）/ transformers（本地模型）
  - 替换 Phase 1 的字符近似估算
  - 统一 estimate_tokens(text, model_type) 接口

### 阶段 7：多仓库 + 文档摄入

- [ ] **7.1** 多仓库采集
  - GitCollector.collect_all_repos()：遍历 config.repositories
  - 每仓库独立采集，per-repo author override
  - _merged/ 按天合并时按时间戳排序（非简单拼接），commit hash 去重

- [ ] **7.2** 文档摄入器
  - src/collectors/doc_collector.py：DocCollector 类
  - 支持格式：.md（直接读取）、.docx（python-docx）、.txt、.pdf（PyMuPDF 可选）
  - wkr ingest --dir ~/notes/ 扫描目录
  - wkr ingest --file report.md 导入单文件
  - echo "..." | wkr ingest 管道输入

- [ ] **7.3** 文档 chunk 索引
  - 摄入后自动做 embedding 追加到 FAISS 索引
  - 非日志类文档走通用 chunk 策略（300 字符 / 50 overlap）
  - chunk metadata：{filename, chunk_index}

- [ ] **7.4** commit message 质量增强
  - wkr log --manual 补充低信息量 commit 的上下文
  - 补充内容追加到 logs/manual/{date}.md
  - Agent prompt 中强化："commit 信息不足时标注 [需补充]，勿编造"

### 阶段 8：模板系统 + DOCX 导出

- [ ] **8.1** 模板管理命令
  - wkr template --list：列出 templates/ 下所有模板
  - wkr template --new：交互式创建自定义模板（生成 .md.j2 + .schema.yaml 骨架）

- [ ] **8.2** 项目管理模板
  - templates/project.md.j2：项目管理周报模板（进度概览表格、里程碑、风险与依赖）
  - templates/project.schema.yaml：对应 Schema（modules、milestones、risks 等变量）

- [ ] **8.3** 极简模板
  - templates/minimal.md.j2：极简版周报（仅核心成果 + 下周计划）

- [ ] **8.4** DOCX 导出增强
  - exporter.py 完善 DOCX 导出：标题层级、表格、列表样式
  - wkr report 后交互选项新增 "导出 DOCX"
  - 输出路径：reports/{start}_{end}_weekly_report.docx

- [ ] **8.5** HTML 导出（可选）
  - exporter.py 支持 HTML 输出
  - 可用于 Web Dashboard 预览（Phase 4 复用）

### 阶段 9：Phase 2 测试验证

- [ ] **9.1** RAG 检索质量测试
  - 积累 2+ 周日志后，验证 search 能找到相关内容
  - 验证子 chunk 去重生效（同一天只召回 1 条）
  - 验证超长 query 降级为摘要检索

- [ ] **9.2** 多仓库测试
  - 配 2 个仓库，验证 _merged/ 合并正确、hash 去重、per-repo author 生效

- [ ] **9.3** 文档摄入测试
  - 摄入 1 个 .md + 1 个 .docx，验证 chunk 索引、wkr search 能召回

- [ ] **9.4** 多模板测试
  - 用 standard / project / minimal 三个模板各生成一次周报
  - 验证 Schema 校验对不同模板的二次校验生效

---

## Phase 3：自动化（1 周）

> 目标：减少人工干预
> 期望成果：周五自动出周报，用户只需审阅

### 阶段 10：定时调度

- [ ] **10.1** CronHelper — Linux/macOS
  - src/scheduler/cron_helper.py：CronHelper 类
  - install()：写入 crontab（带 # wkr-weekly-report 标签）
  - uninstall()：按标签删除 crontab 条目
  - show()：显示当前 cron 配置
  - crontab 内容：0 17 * * 5 cd /path && wkr report --config config.yaml

- [ ] **10.2** CronHelper — Windows
  - _install_windows_schtasks()：schtasks /create 创建计划任务
  - _parse_cron_for_windows()：cron 表达式 → Windows 触发器转换
    - "0 17 * * 5" → /sc weekly /d FRI /st 17:00
    - "30 9 * * 1-5" → /sc weekly /d MON,TUE,WED,THU,FRI /st 09:30
  - 不支持的 cron 语法（L, W, #）报 UnsupportedCronError + 提示

- [ ] **10.3** schedule 命令
  - wkr schedule --install：交互式输入 cron 表达式（默认 0 17 * * 5），安装
  - wkr schedule --uninstall：卸载
  - wkr schedule --show：显示当前配置
  - wkr schedule --test：模拟触发一次 wkr report

- [ ] **10.4** Webhook 通知
  - 支持钉钉/飞书 Webhook URL（config.yaml → schedule.notify.webhook）
  - 周报生成成功后 POST 通知：标题 + 摘要 + 文件路径
  - payload 格式兼容钉钉和飞书的消息卡片

- [ ] **10.5** .env 安全管理
  - 敏感配置（API Key）从 .env 文件读取
  - .gitignore 中排除 .env
  - wkr init 时引导设置 .env

### 阶段 11：Phase 3 测试验证

- [ ] **11.1** Cron 安装/卸载测试
  - Linux：验证 crontab 写入/删除/查看
  - Windows：验证 schtasks 创建/删除（如有环境）

- [ ] **11.2** 定时触发测试
  - wkr schedule --test 验证完整流程
  - 改 cron 为 1 分钟后，验证自动触发

- [ ] **11.3** Webhook 测试
  - 配置测试 Webhook URL，验证 POST 请求发送成功
  - 验证钉钉/飞书消息卡片格式正确

---

## Phase 4：Web Dashboard（2 周，可选）

> 目标：可视化管理界面
> 期望成果：浏览器打开即可管理周报，不碰命令行

### 阶段 12：FastAPI 后端

- [ ] **12.1** FastAPI 应用骨架
  - src/web.py：FastAPI 实例 + CORS + 路由注册
  - /api/health 健康检查

- [ ] **12.2** 配置 API
  - GET /api/config：获取当前配置
  - PUT /api/config：更新配置
  - POST /api/config/check：检查配置完整性

- [ ] **12.3** 日志 API
  - GET /api/logs：获取日志列表（支持日期范围过滤）
  - GET /api/logs/{date}：获取指定日期日志
  - POST /api/logs/manual：手动录入日志

- [ ] **12.4** 周报 API
  - POST /api/reports/generate：触发周报生成（异步任务）
  - GET /api/reports/{id}：获取周报内容
  - GET /api/reports/{id}/json：获取原始 JSON 数据
  - PUT /api/reports/{id}：编辑周报（保存修改后的 Markdown）
  - GET /api/reports/{id}/export：导出 DOCX/HTML
  - GET /api/reports/history：历史周报列表

- [ ] **12.5** 模板 API
  - GET /api/templates：列出模板
  - GET /api/templates/{name}：获取模板内容
  - POST /api/templates：创建自定义模板

- [ ] **12.6** 搜索 API
  - GET /api/search?q=关键词：语义搜索历史

- [ ] **12.7** APScheduler 调度
  - Web 模式专用：进程内定时器（替代系统 cron）
  - POST /api/schedule：设置定时任务
  - GET /api/schedule：查看当前定时配置
  - DELETE /api/schedule：删除定时任务

- [ ] **12.8** 异步任务队列
  - FastAPI BackgroundTasks 或 asyncio.Queue
  - 周报生成为异步任务，前端轮询状态
  - 任务状态：pending → running → completed / failed

### 阶段 13：React 前端

- [ ] **13.1** 前端项目初始化
  - React + Tailwind CSS + Vite
  - 路由：/（首页）、/reports（周报列表）、/reports/:id（周报详情）、/settings（配置）、/logs（日志查看）

- [ ] **13.2** 首页 Dashboard
  - 本周数据摘要卡片（commit 数、文件数、记录天数）
  - 快捷操作：生成周报、查看日志、搜索历史
  - 最近生成的周报列表

- [ ] **13.3** 周报生成页
  - 模板选择
  - 一键生成 + 实时状态（进度条 / 加载动画）
  - 生成后预览（Markdown 渲染）

- [ ] **13.4** 周报编辑页
  - 在线 Markdown 编辑器（或所见即所得）
  - 保存修改 + 重新导出

- [ ] **13.5** 历史周报浏览
  - 按周/月列表
  - 搜索过滤
  - 点击查看详情 + 导出

- [ ] **13.6** 日志查看页
  - 按日历视图浏览日志
  - 手动录入入口

- [ ] **13.7** 配置管理页
  - 表单化配置编辑（不用手写 YAML）
  - LLM 连接测试按钮
  - Embedding 模型选择 + 预下载

### 阶段 14：Phase 4 测试验证

- [ ] **14.1** API 测试
  - 每个 API endpoint 的正常/异常响应测试
  - 异步任务状态流转测试

- [ ] **14.2** 前端 E2E 测试
  - 浏览器端完整流程：配置 → 采集 → 生成 → 编辑 → 导出
  - 响应式布局验证（桌面/平板）

- [ ] **14.3** 部署测试
  - 本地启动：uvicorn src.web:app + npm run dev
  - 验证前后端联调正常

---

## 附：里程碑总览

| 阶段 | 核心交付物 | 验证标准 |
|------|-----------|----------|
| Phase 1 | wkr log --from-git && wkr report | 真实仓库生成可读周报，手动修改率 <20% |
| Phase 2 | 多数据源 + RAG + 多模板 | 2 仓库 + 笔记目录整合，历史上下文有帮助 |
| Phase 3 | wkr schedule --install | 周五自动出周报，无需手动执行 |
| Phase 4 | 浏览器 http://localhost:8000 | 纯浏览器完成完整周报流程 |
